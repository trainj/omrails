<%= form_for(oitem) do |f| %>
  <% if oitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(oitem.errors.count, "error") %> prohibited this oitem from being saved:</h2>

      <ul>
      <% oitem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
      <%= f.label :title %>
      <%= f.text_field :title, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :url %>
    <%= f.text_field :url, class: "form-control" %>
  </div>
  <p><strong>Or</strong></p>
  <div class="form-group">
      <%= f.label :text %>
      <%= f.text_field :text, class: "form-control" %>
  </div>

  <%= f.submit class: "btn btn-primary" %>
<% end %>
